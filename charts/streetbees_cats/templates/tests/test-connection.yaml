apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "street_bees_cats.fullname" . }}-test-connection"
  labels:
    {{- include "street_bees_cats.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "street_bees_cats.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
