apiVersion: v1
kind: Service
metadata:
  name: {{ include "street_bees_cats.fullname" . }}
  labels:
    {{- include "street_bees_cats.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "street_bees_cats.selectorLabels" . | nindent 4 }}
